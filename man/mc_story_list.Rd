% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.r
\name{mc_story_list}
\alias{mc_story_list}
\title{Fetch matching stories}
\usage{
mc_story_list(
  query,
  start_date,
  end_date,
  collection_id = NULL,
  source_id = NULL,
  max_results = Inf,
  page_size = NULL,
  sort_order = NULL,
  expanded = FALSE,
  randomize = FALSE,
  platform = mc_platforms,
  .token = NULL
)
}
\arguments{
\item{query}{the search string, in Elasticsearch/Lucene syntax. \code{"*"}
matches everything.}

\item{start_date, end_date}{the period to search, as \code{Date} objects or
\code{"YYYY-MM-DD"} strings. Both are required.}

\item{collection_id, source_id}{ids to search within, as returned by
\code{\link[=mc_collections]{mc_collections()}} and \code{\link[=mc_sources]{mc_sources()}}. At least one of the two is
required. Vectors are allowed.}

\item{max_results}{how many stories to fetch. Defaults to all of them, which
for a broad query can be a very long pull at two requests per minute — set
a limit when exploring.}

\item{page_size}{how many stories to ask for per request.}

\item{sort_order}{\code{"desc"} (newest indexed first, the default) or \code{"asc"}.}

\item{expanded}{if \code{TRUE}, request the full story text. This is restricted
to staff accounts and will fail with "You are not permitted to fetch
\code{expanded} stories" for everyone else.}

\item{randomize}{if \code{TRUE}, sample pages at random rather than in order.
Also staff only.}

\item{platform}{which archive to query. \code{"onlinenews-mediacloud"} is the
Media Cloud index; \code{"onlinenews-waybackmachine"} searches the Wayback
Machine instead.}

\item{.token}{a token, or path to a saved token file, to use instead of the
stored one.}
}
\value{
A tibble with one row per story: \code{id}, \code{title}, \code{url},
\code{publish_date} (a \code{Date}), \code{indexed_date} (a \code{POSIXct}), \code{language},
\code{media_name} and \code{media_url}.
}
\description{
Returns the story metadata matching a query. Paging is handled
internally: the endpoint hands back a \code{pagination_token} which is fed back
until the archive is exhausted or \code{max_results} is reached.
}
\examples{
\dontrun{
mc_story_list("klima", "2026-08-01", "2026-08-07",
              collection_id = 34412409, max_results = 200)
}

}
