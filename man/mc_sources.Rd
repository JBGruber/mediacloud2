% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/directory.r
\name{mc_sources}
\alias{mc_sources}
\title{List sources}
\usage{
mc_sources(collection_id = NULL, name = NULL, max_results = Inf, .token = NULL)
}
\arguments{
\item{collection_id}{restrict to sources in this collection. This is the
usual way to use the function; see also \code{\link[=mc_collection_sources]{mc_collection_sources()}}.}

\item{name}{search term, matched case-insensitively against the source
name, its label and its alternative domains. Multiple words are combined
with AND.}

\item{max_results}{maximum number of sources to return.}

\item{.token}{a token, or path to a saved token file, to use instead of the
stored one.}
}
\value{
A tibble with one row per source, including \code{id}, \code{name},
\code{label}, \code{homepage}, \code{stories_per_week}, \code{last_story}, \code{pub_country},
\code{pub_state}, \code{primary_language}, \code{media_type} and \code{collection_count}.
}
\description{
Sources are the individual outlets — one row per domain.
}
\details{
Sources come back sorted by \code{stories_per_week}, busiest first.
\code{last_story} is only reported to the month by this endpoint, so it is
returned as the first day of that month.
}
\examples{
\dontrun{
mc_sources(collection_id = 34412409)
mc_sources(name = "spiegel")
}

}
