% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta.r
\name{mc_profile}
\alias{mc_profile}
\title{Your account, roles and quota}
\usage{
mc_profile(.token = NULL)
}
\arguments{
\item{.token}{a token, or path to a saved token file, to use instead of the
stored one.}
}
\value{
A tibble with one row: \code{id}, \code{username}, \code{is_staff},
\code{is_superuser}, \code{groups} (a list column), and the quota columns
\code{quota_provider}, \code{quota_hits}, \code{quota_week} and \code{quota_limit}.
}
\description{
Returns the account the token belongs to. Since almost every
other endpoint requires authentication, this is the call to make when you
want to know whether a token works.
}
\details{
Quota is counted per week and per provider. Endpoints do not all
cost the same: a story list or count costs one hit, \code{\link[=mc_languages]{mc_languages()}} two,
and \code{\link[=mc_words]{mc_words()}}, \code{\link[=mc_attention_by_source]{mc_attention_by_source()}} and \code{\link[=mc_count_by_source]{mc_count_by_source()}}
four.
}
\examples{
\dontrun{
mc_profile()
}

}
