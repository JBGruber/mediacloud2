% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.r
\name{mc_attention_by_source}
\alias{mc_attention_by_source}
\title{Which sources carried the matching stories}
\usage{
mc_attention_by_source(
  query,
  start_date,
  end_date,
  collection_id = NULL,
  source_id = NULL,
  platform = mc_platforms,
  .token = NULL
)
}
\arguments{
\item{query}{the search string, in Elasticsearch/Lucene syntax. \code{"*"}
matches everything.}

\item{start_date, end_date}{the period to search, as \code{Date} objects or
\code{"YYYY-MM-DD"} strings. Both are required.}

\item{collection_id, source_id}{ids to search within, as returned by
\code{\link[=mc_collections]{mc_collections()}} and \code{\link[=mc_sources]{mc_sources()}}. At least one of the two is
required. Vectors are allowed.}

\item{platform}{which archive to query. \code{"onlinenews-mediacloud"} is the
Media Cloud index; \code{"onlinenews-waybackmachine"} searches the Wayback
Machine instead.}

\item{.token}{a token, or path to a saved token file, to use instead of the
stored one.}
}
\value{
A tibble with one row per source, with the source name and the
number of matching stories it carried.
}
\description{
Which sources carried the matching stories
}
\details{
How many sources come back is decided by the server, which passes
its own value positionally; there is no \code{limit} argument to set. Costs
four quota hits.
}
\examples{
\dontrun{
mc_attention_by_source("klima", "2026-08-01", "2026-08-07",
                       collection_id = 34412409)
}

}
