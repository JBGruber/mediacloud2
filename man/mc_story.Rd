% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.r
\name{mc_story}
\alias{mc_story}
\title{Look up a single story}
\usage{
mc_story(id, platform = mc_platforms, .token = NULL)
}
\arguments{
\item{id}{a story id, as returned in the \code{id} column of \code{\link[=mc_story_list]{mc_story_list()}}.}

\item{platform}{which archive to query. \code{"onlinenews-mediacloud"} is the
Media Cloud index; \code{"onlinenews-waybackmachine"} searches the Wayback
Machine instead.}

\item{.token}{a token, or path to a saved token file, to use instead of the
stored one.}
}
\value{
A tibble with one row. Non-staff accounts do not receive the
article text; the \code{text} column is stripped server side.
}
\description{
Look up a single story
}
\examples{
\dontrun{
stories <- mc_story_list("klima", "2026-08-01", "2026-08-07",
                         collection_id = 34412409, max_results = 1)
mc_story(stories$id[1])
}

}
