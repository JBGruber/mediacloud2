% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.r
\name{mc_count_by_source}
\alias{mc_count_by_source}
\title{Attention per source over time}
\usage{
mc_count_by_source(
  query,
  start_date,
  end_date,
  collection_id = NULL,
  source_id = NULL,
  interval = c("week", "day", "month", "year"),
  platform = mc_platforms,
  .token = NULL
)
}
\arguments{
\item{query}{the search string, in Elasticsearch/Lucene syntax. \code{"*"}
matches everything.}

\item{start_date, end_date}{the period to search, as \code{Date} objects or
\code{"YYYY-MM-DD"} strings. Both are required.}

\item{collection_id, source_id}{ids to search within, as returned by
\code{\link[=mc_collections]{mc_collections()}} and \code{\link[=mc_sources]{mc_sources()}}. At least one of the two is
required. Vectors are allowed.}

\item{interval}{size of the time buckets: \code{"day"}, \code{"week"}, \code{"month"} or
\code{"year"}.}

\item{platform}{which archive to query. \code{"onlinenews-mediacloud"} is the
Media Cloud index; \code{"onlinenews-waybackmachine"} searches the Wayback
Machine instead.}

\item{.token}{a token, or path to a saved token file, to use instead of the
stored one.}
}
\value{
A tibble with one row per source and bucket: \code{media_name},
\code{interval}, \code{bucket}, \code{matching_stories}, \code{total_stories} and \code{ratio}.
}
\description{
Splits the matching stories by source and by time bucket, which
is what the "attention over time by source" view of the web app shows.
}
\details{
The server refuses queries whose number of sources times number of
buckets is too large, so narrow the period or use a coarser \code{interval}
when searching a big collection. This endpoint costs four quota hits.
}
\examples{
\dontrun{
mc_count_by_source("klima", "2026-08-01", "2026-08-31",
                   collection_id = 34412409, interval = "week")
}

}
